FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
ARG VITE_CLIENT_URL
ENV VITE_CLIENT_URL=$VITE_CLIENT_URL
ARG ACCESS_TOKEN_SECRET
ENV ACCESS_TOKEN_SECRET=$ACCESS_TOKEN_SECRET
ARG SYNC_INDEXES
ENV SYNC_INDEXES=$SYNC_INDEXES

EXPOSE 3000
CMD ["npm", "start"]
